# Шаблон Secret — реальные значения задавайте через kubectl/Sealed Secrets, не коммитьте в git
apiVersion: v1
kind: Secret
metadata:
  name: mental-service-secrets
  namespace: healthlife
type: Opaque
stringData:
  SPRING_DATASOURCE_PASSWORD: "CHANGE_ME"
  JWT_SECRET: "CHANGE_ME_MIN_32_BYTES_BASE64_OR_HEX"
  ANTHROPIC_API_KEY: "CHANGE_ME"
