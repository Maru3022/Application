server:
  port: 0

spring:
  application:
    name: gateway-service-test
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisReactiveAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisRepositoriesAutoConfiguration

management:
  health:
    redis:
      enabled: false

jwt:
  secret: TestSecretKeyThatIsAtLeast256BitsLongForHS256Algorithm2025TestTest
  access-token:
    expiration: 900000
  refresh-token:
    expiration: 604800000

gateway:
  routes:
    auth: http://localhost:19999
    users: http://localhost:19999
    health: http://localhost:19999
    mental: http://localhost:19999
    nutrition: http://localhost:19999
    ai: http://localhost:19999
    social: http://localhost:19999
    notifications: http://localhost:19999
    analytics: http://localhost:19999
    payments: http://localhost:19999

cors:
  allowed-origins: http://localhost:3000

resilience4j:
  circuitbreaker:
    configs:
      default:
        sliding-window-size: 10
        failure-rate-threshold: 50
        wait-duration-in-open-state: 1000
  retry:
    configs:
      default:
        max-attempts: 1
        wait-duration: 100
  ratelimiter:
    configs:
      default:
        limit-for-period: 1000
        limit-refresh-period: 1s
        timeout-duration: 100ms
    instances:
      gateway:
        base-config: default
